#include <stdlib.h>
#include <stdio.h>

// -- escreva seu código abaixo dessa linha --- (e não altere esta linha)

int main()
{
  int b, h, a;
  scanf("%d %d", &b, &h);
  a = (b * h) / 2;
  printf("%d", a);
}